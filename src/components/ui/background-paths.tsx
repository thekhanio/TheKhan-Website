/**
 * BackgroundPaths — neutralized in the v2 editorial design system.
 * Originally rendered 72 looping SVG curves at 30% opacity (Aceternity
 * pattern). Now a no-op so private pages that still import it compile.
 * Public pages no longer use it.
 */
export function BackgroundPaths() {
  return null;
}
